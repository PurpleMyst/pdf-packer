{
  "name": "label-pack",
  "version": "1.0.0",
  "description": "PDF page packer for A4 labels",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "pdf-lib": "^1.17.1",
    "vitest": "^1.6.0"
  }
}